# Crosses v2 (gggw) — crosses_v2_left / crosses_v2_right boards.
#
# The board's own defconfig and crosses_v2.dts already cover the trackballs,
# split roles, battery reporting and BLE tuning. Keep this file for user
# preferences only.

CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000
